# Development image for test / lint / fmt — no binary built, just the toolchain.
FROM rust:1.93-slim

RUN apt-get update && \
    apt-get install -y pkg-config libssl-dev && \
    rm -rf /var/lib/apt/lists/* && \
    rustup component add clippy rustfmt

WORKDIR /app
